---
phase: 01-foundation-and-configuration
plan: 05
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified: []
autonomous: false

requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05

must_haves:
  truths:
    - "User launches app for the first time and wizard appears"
    - "API key validation blocks advancement on failure with correct error message"
    - "Microphone list shows real system sources (no monitor sources)"
    - "Vocabulary list persists across app restarts"
    - "Config file exists at correct path with 0600 permissions after wizard completes"
    - "Second launch while app is running is silently ignored"
    - "Settings window re-enumerates mics and shows saved config values"
  artifacts:
    - path: "~/.config/linux-speech-flow/config.json"
      provides: "Persisted config after wizard completes"
      contains: "setup_complete"
  key_links:
    - from: "linux-speech-flow (CLI)"
      to: "wizard window"
      via: "app.py do_activate checks setup_complete"
      pattern: "n/a (interactive)"
---

<objective>
Human verification of the complete Phase 1 flow: wizard → validate → configure → persist → restart.

Purpose: Automated tests cannot verify GTK rendering, interactive flows, or actual PulseAudio device enumeration. Human eyes confirm the product works end-to-end.
Output: Confirmed working wizard flow; any issues captured for gap closure.
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify complete Phase 1 wizard flow end-to-end</name>
  <action>Human runs the wizard flow and verifies all Phase 1 requirements are met.</action>
  <what-built>
    Complete Phase 1: project scaffold, config persistence (TDD), Groq validation (TDD), GTK wizard (API key → mic → vocabulary), settings window, and app startup sequence. All automated tests pass.
  </what-built>
  <how-to-verify>
    Before starting, reset to fresh state:
    ```bash
    rm -f ~/.config/linux-speech-flow/config.json
    ```

    **Test 1: First-run wizard appears**
    1. Run: `linux-speech-flow`
    2. Expected: Setup wizard window appears (not a tray icon, not an error)
    3. Expected: Window title indicates "Setup" or similar, shows Step 1 (API key)

    **Test 2: API key validation — invalid format**
    1. Enter "not-a-groq-key" in the API key field
    2. Click Next (or Validate)
    3. Expected: Error appears inline (not a popup dialog): "Invalid API key format" or similar
    4. Expected: You remain on Step 1, cannot advance

    **Test 3: API key validation — network error simulation (optional)**
    1. Temporarily disconnect from internet (or use a firewall rule)
    2. Enter "gsk_" + 50 chars of any text
    3. Click Next
    4. Expected: Spinner appears, Next is disabled
    5. Expected: Error appears: "Could not connect to Groq — check your internet connection"

    **Test 4: API key validation — real key (if available)**
    1. Enter a real Groq API key (gsk_...)
    2. Click Next
    3. Expected: Spinner appears briefly, then advances to Step 2 (Microphone)

    **Test 5: Microphone selection**
    1. On Step 2: Microphone dropdown shows at least one real microphone source
    2. Expected: No "Monitor of" entries in the dropdown
    3. Select a microphone, click Next
    4. Expected: Advances to Step 3 (Vocabulary)

    **Test 6: Vocabulary step (skip)**
    1. Leave vocabulary empty
    2. Click Finish
    3. Expected: Wizard closes

    **Test 7: Config persisted**
    ```bash
    cat ~/.config/linux-speech-flow/config.json
    ls -la ~/.config/linux-speech-flow/config.json
    ```
    Expected: File exists, contains `"setup_complete": true`, permissions show `-rw-------` (0600)

    **Test 8: Second launch ignored**
    1. Run `linux-speech-flow` again (while first instance still running, if applicable, or after it closed)
    2. If first instance already exited after wizard: relaunch should show placeholder window (setup complete)
    3. If first instance is running: second launch exits silently

    **Test 9: Settings window**
    1. From the placeholder window (visible after wizard finishes), click "Open Settings"
    2. Expected: Settings window opens with saved API key, saved microphone selected, saved vocabulary populated
    3. Change vocabulary to: `kubernetes\nOAuth\nlinux-speech-flow`
    4. Click Save
    5. Relaunch app
    6. Open Settings again
    7. Expected: Vocabulary shows the three words

    **Test 10: Wizard re-opens if incomplete**
    ```bash
    # Simulate incomplete setup
    python3 -c "
    import json
    from pathlib import Path
    p = Path.home() / '.config/linux-speech-flow/config.json'
    c = json.loads(p.read_text())
    c['setup_complete'] = False
    p.write_text(json.dumps(c, indent=2))
    "
    linux-speech-flow
    ```
    Expected: Wizard appears again (not the placeholder window)
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any failures found</resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria from ROADMAP.md must be TRUE:
1. GTK wizard appears on first launch
2. API key validated against Groq before wizard advances
3. Microphone selectable from PulseAudio/PipeWire sources (no monitor sources)
4. Vocabulary persists across restarts
5. Config at ~/.config/linux-speech-flow/config.json with 0600 permissions
</verification>

<success_criteria>
Human approves all 10 verification tests. Any failures trigger gap closure planning.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-configuration/01-05-SUMMARY.md`
</output>
