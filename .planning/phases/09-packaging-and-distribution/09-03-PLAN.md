---
phase: 09-packaging-and-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-03
  - DIST-05

must_haves:
  truths:
    - "README documents all required system dependencies for both the .deb path and the pip path"
    - "README explains that autostart at login is handled automatically by the app at runtime (satisfying DIST-03)"
    - "System dep section appears prominently before installation instructions"
    - "Both installation methods (.deb and pip) are documented"
  artifacts:
    - path: "README.md"
      provides: "System dependency documentation and installation guide"
      contains: "xdotool"
  key_links:
    - from: "README.md system deps section"
      to: "xdotool, xclip, libnotify-bin, libpulse0, gir1.2-appindicator3-0.1"
      via: "apt-get install command"
      pattern: "xdotool.*xclip.*libnotify-bin"
---

<objective>
Create README.md with system dependency documentation, covering both the .deb installation path and the pip/developer path, and noting that autostart at login is configured automatically at runtime.

Purpose: Satisfies DIST-05 (README documents required system dependencies) and provides installation instructions for users on both paths. DIST-03 (autostart at login) is already satisfied by existing runtime behavior in app.py — README documents this fact.
Output: README.md with installation guide and system dep section
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-packaging-and-distribution/09-CONTEXT.md
@.planning/phases/09-packaging-and-distribution/09-RESEARCH.md

<interfaces>
<!-- System deps for each install path (from CONTEXT.md / RESEARCH.md) -->

.deb path runtime deps (handled by fpm --depends, documented for clarity):
- xdotool
- xclip
- libnotify-bin
- libpulse0
- gir1.2-appindicator3-0.1 | gir1.2-ayatanaappindicator3-0.1

pip path (developer install) — build-time system deps:
- libgirepository1.0-dev
- gir1.2-gtk-3.0
- libcairo2-dev
- pkg-config
- libpulse-dev
- xdotool
- xclip
- libnotify-bin

Autostart: app writes ~/.config/autostart/linux-speech-flow.desktop at first launch (Phase 4).
PyPI package name: linux-speech-flow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with installation guide and system dependency documentation</name>
  <files>README.md</files>
  <action>
    Create README.md (the file does not exist yet). Follow the project's concise style (no emojis, no preamble). Write with this structure and content:

    - Title: "# linux-speech-flow"
    - Tagline: "Hold a key, speak, release — transcribed text appears in whatever you're typing in."
    - Platform: "Runs on Ubuntu 22.04+, Debian 12+, Pop!_OS 22.04+ (X11 session required)."
    - "## Requirements" section with "### System dependencies" subsection containing an apt-get install block for: xdotool xclip libnotify-bin libpulse0 gir1.2-appindicator3-0.1. Include a note that gir1.2-ayatanaappindicator3-0.1 is the alternative for non-GNOME desktops.
    - "## Installation" section with two subsections:
      - "### Option 1: .deb package (recommended)" — download from GitHub Releases, then `sudo apt install ./linux-speech-flow_*.deb`. Note that deps are installed automatically.
      - "### Option 2: pip (developer install)" — apt-get install block for: libgirepository1.0-dev libcairo2-dev pkg-config gir1.2-gtk-3.0 libpulse-dev xdotool xclip libnotify-bin. Then `pip install linux-speech-flow`.
    - "## First launch" section: explain the setup wizard, and explicitly state that the app creates `~/.config/autostart/linux-speech-flow.desktop` at first launch so it starts automatically at login.
    - "## Usage" section with a hotkey table: F9 (start/stop dictation), ESC (stop recording), F10 (reprocess failed), F11 (start/stop conversation), F12 (send feedback), Ctrl+Alt+H (toggle Slack huddle). Note hotkeys are configurable in Settings.
    - "## Features" section: bullet list of key features (Groq Whisper transcription, LLM post-processing, custom vocabulary, system tray, history viewer, conversation mode with AI analysis, Slack integration, configurable hotkeys).
    - "## License" section: "MIT"
  </action>
  <verify>
    <automated>cd /home/jon/projects/linux-speech-flow && test -f README.md && grep -q "xdotool" README.md && grep -q "xclip" README.md && grep -q "libnotify-bin" README.md && grep -q "libpulse0" README.md && grep -q "gir1.2-appindicator3-0.1" README.md && grep -q "libgirepository1.0-dev" README.md && grep -q "autostart" README.md && echo "README.md OK" && pytest --tb=short -q</automated>
  </verify>
  <done>
    README.md exists and contains: all 5 runtime system deps (xdotool, xclip, libnotify-bin, libpulse0, gir1.2-appindicator3-0.1), pip path build deps (libgirepository1.0-dev + others), autostart explanation, both install methods (.deb and pip). Existing tests still pass.
  </done>
</task>

</tasks>

<verification>
- `grep -q "xdotool" README.md && grep -q "libnotify-bin" README.md && grep -q "libgirepository" README.md` — all pass
- `grep -q "autostart" README.md` — confirms DIST-03 runtime behavior is documented
- `pytest --tb=short -q` — full suite green
</verification>

<success_criteria>
- README.md exists with a System dependencies section listing: xdotool, xclip, libnotify-bin, libpulse0, gir1.2-appindicator3-0.1 (with ayatana alternative noted)
- README.md documents pip path build deps including libgirepository1.0-dev
- README.md explains that autostart is written at first launch by the app
- README.md includes both .deb and pip install methods
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-and-distribution/09-03-SUMMARY.md`
</output>
