---
phase: 09-packaging-and-distribution
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified: []
autonomous: false
requirements:
  - DIST-01
  - DIST-04

must_haves:
  truths:
    - "CI build job passes on GitHub (test + build jobs both green)"
    - "PyPI trusted publisher is configured on pypi.org before first v* tag is pushed"
    - ".deb artifact is uploaded to GitHub Actions as a downloadable artifact"
    - "Wheel artifact is uploaded to GitHub Actions as a downloadable artifact"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI/CD pipeline"
      contains: "pypa/gh-action-pypi-publish"
  key_links:
    - from: "GitHub Actions CI"
      to: "PyPI trusted publishing"
      via: "pypa/gh-action-pypi-publish OIDC"
      pattern: "id-token: write"
---

<objective>
Verify the CI build passes on GitHub after the pyproject.toml and ci.yml changes, and configure PyPI trusted publishing so the publish job will succeed on the first v* tag push.

Purpose: Validates that the .deb and wheel build artifacts are produced correctly by CI, and pre-configures PyPI before any tag is pushed (avoiding the chicken-and-egg problem).
Output: Green CI build, PyPI trusted publisher configured
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-packaging-and-distribution/09-CONTEXT.md
@.planning/phases/09-packaging-and-distribution/09-RESEARCH.md
@.planning/phases/09-packaging-and-distribution/09-02-SUMMARY.md
@.planning/phases/09-packaging-and-distribution/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit all phase changes and push to trigger CI</name>
  <files></files>
  <action>
    Run the full test suite locally to confirm all tests still pass, then commit and push all Phase 9 changes to GitHub.

    Steps:
    1. Run `pytest --tb=short -q` — must be green before committing
    2. Stage all changed files: pyproject.toml, scripts/postinst.sh, scripts/postrm.sh, .github/workflows/ci.yml, README.md
    3. Commit with message: `feat(09): packaging — setuptools-scm, postinst/postrm, CI fixes, README`
    4. Push to the current branch with `git push`
    5. Run `gh run list --limit 1` to get the run ID, then `gh run watch <ID>` to monitor CI
    6. Wait for the `test` and `build` jobs to complete — both must be green

    The `publish` job will be skipped (it only runs on `v*` tags) — this is expected and correct.
    If the build job fails, diagnose with `gh run view --log-failed`.
  </action>
  <verify>
    <automated>cd /home/jon/projects/linux-speech-flow && pytest --tb=short -q</automated>
  </verify>
  <done>Local tests pass. All Phase 9 changes committed and pushed. CI `test` and `build` jobs complete with green status. .deb and wheel artifacts downloadable from the GitHub Actions run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CI artifacts and configure PyPI trusted publisher</name>
  <files></files>
  <action>Human verification checkpoint — see how-to-verify instructions below.</action>
  <verify>
    <automated>MANUAL — see how-to-verify steps below</automated>
  </verify>
  <done>CI is green, .deb artifact has correct metadata, PyPI trusted publisher is configured.</done>
  <what-built>
    All Phase 9 packaging changes:
    - pyproject.toml: setuptools-scm versioning
    - scripts/postinst.sh + scripts/postrm.sh: fpm maintainer scripts
    - .github/workflows/ci.yml: fixed version extraction, PyGObject pin, fpm flags
    - README.md: system dep documentation and install guide
    CI has built both .deb and wheel artifacts
  </what-built>
  <how-to-verify>
    1. Open GitHub Actions for this repo — confirm latest CI run shows `test` (Python 3.10 and 3.12) and `build` jobs all green
    2. In the `build` job, click "Artifacts" and confirm both `linux-speech-flow-deb` and `linux-speech-flow-wheel` are listed
    3. Download the .deb artifact and run: `dpkg-deb --info linux-speech-flow_*.deb` — verify package name `linux-speech-flow`, version is not `0.1.0` static (should be from git), and depends includes xdotool, xclip, libnotify-bin, libpulse0
    4. Configure PyPI trusted publisher (one-time manual step — cannot be automated):
       - Go to https://pypi.org/manage/account/publishing/
       - Under "Add a new pending publisher", enter:
         - PyPI project name: `linux-speech-flow`
         - Owner: your GitHub username or org
         - Repository name: `linux-speech-flow`
         - Workflow filename: `ci.yml`
         - Environment name: (leave blank)
       - Click "Add"
    5. Confirm the pending publisher appears under "Pending publishers" in your PyPI account
  </how-to-verify>
  <resume-signal>Type "approved" when CI is green and PyPI trusted publisher is configured, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase complete when:
- CI test + build jobs are green on GitHub
- .deb artifact downloadable with correct package metadata (name, deps, non-static version)
- PyPI pending trusted publisher configured for linux-speech-flow/ci.yml
</verification>

<success_criteria>
- GitHub Actions CI: `test` job passes for Python 3.10 and 3.12; `build` job produces .deb and wheel artifacts
- `dpkg-deb --info` on the .deb artifact shows: package name `linux-speech-flow`, runtime depends including xdotool and libpulse0
- PyPI trusted publisher configured at pypi.org for this repo's ci.yml
- All 259 existing tests green locally
</success_criteria>

<output>
After completion, create `.planning/phases/09-packaging-and-distribution/09-04-SUMMARY.md`
</output>
