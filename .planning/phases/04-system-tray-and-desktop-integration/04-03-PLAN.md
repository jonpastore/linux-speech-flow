---
phase: 04-system-tray-and-desktop-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false

requirements:
  - TRAY-01
  - TRAY-02
  - TRAY-03
  - TRAY-04

must_haves:
  truths:
    - "Tray icon appears in system tray on launch"
    - "Tray icon animates during recording (red pulse)"
    - "Tray icon animates during transcription (yellow spin)"
    - "Tray icon shows error state on pipeline failure"
    - "Tray menu opens with correct items in correct order"
    - "Settings menu item opens SettingsWindow"
    - "Open Debug Log menu item opens DebugWindow"
    - "Reprocess Failed is greyed out when no failed WAVs exist"
    - "Quit menu item exits the app cleanly"
    - "Left-click on tray icon opens SettingsWindow"
  artifacts:
    - path: "~/.config/autostart/freeflow.desktop"
      provides: "XDG autostart entry"
  key_links:
    - from: "F9 keypress"
      to: "tray icon red animation"
      via: "_on_recording_start -> TrayManager.set_state('recording')"
      pattern: "set_state.*recording"
    - from: "ESC keypress"
      to: "tray icon yellow animation"
      via: "_on_recording_complete -> TrayManager.set_state('processing')"
      pattern: "set_state.*processing"
---

<objective>
Human verification of the complete Phase 4 system tray integration — confirm tray icon, state animations, menu actions, and error behavior all work as specified.

Purpose: Gates Phase 4 completion; Phase 5 depends on the tray menu "View Run Log" stub being correct.
Output: Verified Phase 4 deliverables, Phase 4 marked complete.
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-system-tray-and-desktop-integration/04-CONTEXT.md
@.planning/phases/04-system-tray-and-desktop-integration/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification checks (automated)</name>
  <files><!-- no files modified — read-only verification task --></files>
  <action>
    Run the following pre-checks before asking for human verification:

    1. Run all existing tests to confirm no regressions:
       ```
       .venv/bin/pytest tests/ -v 2>&1 | tail -20
       ```
       All tests must pass. If any test fails, fix before proceeding to checkpoint.

    2. Confirm tray icon files are installed:
       ```
       ls ~/.local/share/icons/hicolor/scalable/status/freeflow-*.svg | wc -l
       ```
       Must print 8.

    3. Confirm autostart file is valid:
       ```
       cat ~/.config/autostart/freeflow.desktop
       ```
       Must show [Desktop Entry] with correct Exec= line.

    4. Confirm app starts without error (5 second smoke test):
       ```
       timeout 5 .venv/bin/python -m linux_speech_flow 2>&1 || true
       ```
       Should not show ImportError or Python tracebacks. Tray registration errors are expected if no display — run manually for visual check.

    Report results of each check in the summary.
  </action>
  <verify>
    All 4 checks pass without errors.
  </verify>
  <done>
    Pre-checks confirm: tests pass, 8 icons installed, autostart valid, app starts without Python errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of tray icon, state animations, and menu actions</name>
  <files></files>
  <action>
    What was built:
    - TrayManager with 4-state animated tray icon (idle/recording/processing/error)
    - Flat tray menu: Settings / Open Debug Log / Reprocess Failed (N) / Quit
    - Left-click on tray icon opens Settings window
    - App runs headless (no placeholder window)
    - XDG autostart entry at ~/.config/autostart/freeflow.desktop

    Launch: `.venv/bin/python -m linux_speech_flow`

    Scenario 1 — Tray icon appears: Confirm grey circle in system tray. If missing: gnome-extensions list --enabled | grep appindicator
    Scenario 2 — Left-click: Opens SettingsWindow
    Scenario 3 — Right-click menu: Shows Settings / Open Debug Log / Reprocess Failed (0) [greyed] / Quit
    Scenario 4 — Open Debug Log: Opens DebugWindow with log entries
    Scenario 5 — Recording animation: F9 → red pulse; stop → yellow spin during transcription; idle after paste
    Scenario 6 — Error state: Set invalid API key, record and stop, confirm red X icon + desktop notification; F9 again clears error; restore key
    Scenario 7 — Reprocess count: After error, menu shows "Reprocess Failed (1)" enabled; clicking reprocesses and count returns to 0
    Scenario 8 — Quit: Right-click → Quit exits cleanly, icon disappears
  </action>
  <verify>All 8 scenarios pass.</verify>
  <done>Human types "approved" confirming all 8 scenarios pass.</done>
  <resume-signal>Type "approved" if all 8 scenarios pass, or describe any failures with scenario number</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when human approves all 8 verification scenarios.
</verification>

<success_criteria>
- Tray icon visible and animated per pipeline state
- All tray menu items functional
- App runs headless (no window without explicit open action)
- XDG autostart installed and points to correct Python
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-system-tray-and-desktop-integration/04-03-SUMMARY.md`
</output>
