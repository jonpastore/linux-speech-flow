---
phase: 08-slack-integration
task: post-phase-bugs
total_tasks: 4
status: in_progress
last_updated: "2026-03-04T09:50:22.912Z"
---

<current_state>
Phase 8 is complete (all 6 plans done, 332 tests pass). This session fixed post-phase bugs discovered during live testing of the huddle flow. Three bug fixes were made this session, with one partial fix still in effect.
</current_state>

<completed_work>

- Bug 1: Analyse button did nothing — huddle metadata missing `models_used` key caused silent KeyError in `coalesce_file()`. Fixed by adding `"models_used": ""` to both metadata dicts in `huddle_manager.py` (session-complete and mid-session analyze). 332 tests pass.

- Bug 2: Speak button in ConversationQAWindow had no recording/transcribing feedback — buttons were re-enabled before transcription started. Fixed: status label shows "Recording... (stops on silence)" during recording, "Transcribing..." while Groq runs; buttons stay disabled through full cycle.

- Bug 3: Double Conversation Analysis dialog — two independent root causes fixed:
  1. `_finish_session` in `huddle_manager.py` added `_session_finished` one-shot guard (reset on `start_session`) — prevents re-entry from race condition with `_on_in_flight_done`
  2. `app.py` added `self._huddle_dialog` singleton tracker — `_on_huddle_session_complete` closes any existing dialog before opening new one; `_on_huddle_analyze` returns early if dialog already open

- Bug 4 (partial): VU meter + threshold slider added to `HuddleStatusWindow` to allow calibration and prevent Whisper hallucinations ("I love you", "Thank you") from near-silence audio:
  - `HuddleStatusWindow.__init__` now accepts `on_threshold_changed` callback
  - VU meter DrawingArea (same blue/orange visual as ConversationStatusWindow) added
  - Threshold slider added
  - `update_mic_level()`, `set_threshold_from_calibration()` methods added
  - `HuddleRecorder.set_threshold()` added (delegates to inner ConversationRecorder)
  - `HuddleManager` wires `on_audio_level` and `on_threshold_calibrated` callbacks through to status window
  - `_on_threshold_changed` method in HuddleManager syncs slider → recorder

</completed_work>

<remaining_work>

- Hallucination filtering: If calibrating the threshold via the slider doesn't fully prevent "I love you" / "Thank you" hallucinations, may need to add a minimum-RMS pre-filter in `huddle_manager._on_chunk_ready` that skips sending near-silence WAV chunks to Whisper (check average RMS of the WAV before dispatching). Conversation mode has the same issue but is lower priority.

- Phase 9 (Packaging & Distribution) planning: Phase 9 plans are TBD. No CONTEXT.md exists yet. Run `/gsd:discuss-phase 9` or `/gsd:plan-phase 9` to start.

</remaining_work>

<decisions_made>

- `_finish_session` guard uses `_session_finished` bool (reset on `start_session`) rather than clearing `_session_ending` alone — belt-and-suspenders: handles both race condition and any future re-entry path
- `_huddle_dialog` singleton in app.py: `_on_huddle_session_complete` closes existing dialog (session supersedes mid-session analyze); `_on_huddle_analyze` ignores if dialog open (user should close first)
- VU meter reuses exact same `_draw_mic` logic and `RMS_DISPLAY_SCALE` constant from ConversationStatusWindow — no duplication of rendering logic
- `HuddleStatusWindow` size increased from 380→440px to accommodate VU + slider

</decisions_made>

<blockers>

- None blocking. Live testing needed to confirm double-dialog and hallucination fixes work end-to-end.

</blockers>

<context>
All three live-reported bugs are fixed and tests pass (332). The VU meter / threshold slider is wired up — user needs to calibrate threshold to eliminate hallucinations during huddle recording. The approach mirrors conversation mode exactly. Phase 8 bugs are resolved; next logical step is Phase 9 planning.
</context>

<next_action>
Start with: `/clear` then `/gsd:resume-work` — will show Phase 9 as next. Run `/gsd:discuss-phase 9` or `/gsd:plan-phase 9` to begin packaging.
</next_action>
