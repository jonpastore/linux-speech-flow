---
phase: 08-slack-integration
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-04T07:24:01.858Z
---

<current_state>
Mid-session UI/UX improvement pass on Phase 8. Multiple bugs fixed and new features
partially implemented. 259 tests pass. The remaining work is wiring _on_huddle_analyze()
and the channel picker into app.py — all other pieces are done.
</current_state>

<completed_work>

## Bugs fixed this session:
- Audio corruption: latency_msec=1 on PulseAudio loopbacks → changed to 50ms in huddle_recorder.py
- PulseAudio leak on app exit: do_shutdown in app.py now calls _huddle_manager.stop_session()
- atexit cleanup: huddle_recorder.py registers _emergency_cleanup() to unload lsf-huddle-mix on crash/kill
- Transcription crash: s.avg_logprob on dict segments → s.get("avg_logprob", 0) in huddle_manager.py
- WELCOME_MESSAGE NameError: _post_welcome used deleted constant → now calls _make_welcome_message(word)
- CheckButton.set_wrap_mode crash: removed invalid call in conversation_dialog.py
- detect_activation punctuation: text normalized before matching — "Lucifer, stop recording." now works
- Short aliases: stop → stop recording, start → start recording
- Help text: uses configured activation word from config (not hardcoded conyo)
- Pause/resume redesign: no longer stops PulseAudio — just gates _chunk_texts.append with _paused flag

## New features implemented (259 tests pass, NOT YET COMMITTED):
- slack_manager.get_channels(): paginated channel list for workspace
- Debug command with levels in huddle_manager.py:
  - axiom debug on/off/low/medium/high
  - LOW: commands + errors; MEDIUM: + state changes; HIGH: + per-chunk stats
  - _debug_post() helper threads all Slack posts
  - status command includes paused state + debug level
- trigger_analyze() on HuddleManager: pauses transcript, posts pause msg to channel, calls on_analyze callback
- resume_from_analyze() on HuddleManager: resumes transcript, posts resume msg to channel
- on_analyze param on HuddleManager.__init__
- Analyze button on HuddleStatusWindow (huddle_status.py): disabled until chunk_count > 0; set_on_analyze() setter
- on_analyze=self._on_huddle_analyze wired in app.py HuddleManager construction (method stub not yet written)

</completed_work>

<remaining_work>

## Task: Wire app.py analyze flow (the only remaining work)

### 1. Wire Analyze button in HuddleManager.start_session()
In huddle_manager.py, find where _status_window is created in start_session() and add:
  self._status_window.set_on_analyze(self.trigger_analyze)

### 2. Add _on_huddle_analyze(self, transcript, metadata) to app.py
- Called by HuddleManager.trigger_analyze() when Analyze button clicked
- Shows ConversationDialog with current transcript snapshot (same shape as _on_huddle_session_complete)
- On dialog close/cancel: call self._huddle_manager.resume_from_analyze()
- On dialog submit: call _on_huddle_dialog_submit(...) as normal
- NOTE: Check conversation_dialog.py for close/cancel hook — may need connect("destroy", ...) on the window

### 3. Replace auto-post in _on_huddle_analysis_complete with channel picker
Current _on_huddle_analysis_complete (app.py ~line 426) auto-posts immediately to metadata["channel_id"].
Replace with a small channel picker Gtk.Window(modal=True):
- Calls self._slack_manager.get_channels(team_id) in a background thread to populate dropdown
- Gtk.ComboBoxText with channel names
- Defaults to huddle's channel_id from metadata
- "Post to Slack" + "Cancel" buttons
- On Post: calls self._huddle_manager.post_huddle_results(team_id, selected_channel_id, result, saved_path)
- On Cancel: just closes (file already saved locally)

</remaining_work>

<decisions_made>

- Pause = transcript-gate only: recording always continues; _paused only gates _chunk_texts.append
- Analyze button disabled at start: enabled on first chunk; always posts pause/resume message to channel
- Channel picker placement: after Q&A completes (not in ConversationDialog) — user confirmed "After analysis completes"
- Channel picker default: huddle channel, user can redirect to any channel in workspace
- Debug on = LOW: axiom debug on defaults to LOW level (user confirmed)
- Debug posts via background thread: _debug_post always threads to avoid blocking GTK main thread

</decisions_made>

<blockers>

- None. All pieces in place. Just app.py wiring left.

</blockers>

<context>
All the hard implementation work is done. 259 tests pass. The remaining task is purely
app.py plumbing (~60-80 lines).

Key: trigger_analyze() already sets _paused=True and posts to channel before calling on_analyze.
So _on_huddle_analyze() just needs to show the dialog and call resume_from_analyze() on close.

ConversationDialog may not have an explicit on_cancel callback — use connect("destroy", ...) on
the dialog window to ensure resume_from_analyze() always gets called when dialog closes.

After completing this, address user /rc question:
User asked why /rc is not available and being overridden by /gsd autocomplete. This is about
Claude Code skill/command registration — check ~/.claude/settings.json or installed skills.
</context>

<next_action>
1. Read src/linux_speech_flow/conversation_dialog.py to find close/cancel hook
2. Find where _status_window is created in huddle_manager.py start_session()
3. Write the three app.py changes (wire button, add _on_huddle_analyze, channel picker)
4. Run pytest, commit
5. Answer /rc question
</next_action>
