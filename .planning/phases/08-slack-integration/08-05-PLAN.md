---
phase: 08-slack-integration
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - tests/test_slack_manager.py
  - tests/test_huddle_recorder.py
  - tests/test_huddle_manager.py
  - tests/test_slack_socket.py
autonomous: true
requirements: [SLACK-01, SLACK-02, SLACK-03, SLACK-04, SLACK-05]

must_haves:
  truths:
    - "All 4 new test files pass with no failures"
    - "detect_activation is tested for all 11 command variants"
    - "HuddleRecorder teardown guarantee tested (finally block fires even on recorder error)"
    - "SlackSocket user filtering tested (only authed_user_id triggers callback)"
    - "post_huddle_results tested for Block Kit structure and upload_file call"
    - "All pre-existing tests continue to pass"
  artifacts:
    - path: "tests/test_slack_manager.py"
      provides: "SlackManager unit tests: verify_token, add/get/remove_workspace, post_message, upload_file"
    - path: "tests/test_huddle_recorder.py"
      provides: "HuddleRecorder: null-sink setup/teardown, crash recovery, finally-block guarantee"
    - path: "tests/test_huddle_manager.py"
      provides: "detect_activation: all 11 commands, case-insensitive, prefix commands, no-match"
    - path: "tests/test_slack_socket.py"
      provides: "SlackSocket: daemon thread, user filter, ACK always sent"
  key_links:
    - from: "tests/test_huddle_manager.py"
      to: "src/linux_speech_flow/huddle_manager.py"
      via: "import detect_activation"
      pattern: "from linux_speech_flow.huddle_manager import detect_activation"
    - from: "tests/test_huddle_recorder.py"
      to: "src/linux_speech_flow/huddle_recorder.py"
      via: "mocked subprocess.run and pulsectl.Pulse"
      pattern: "mock.*subprocess"
---

<objective>
Complete and harden all Phase 8 test files to ensure full test coverage for the new modules. Confirm regression-free test suite.

Purpose: Plans 01-03 created test stubs; this plan fills gaps, adds edge cases, and verifies the full test suite passes together.
Output: Completed test files; test run confirming 100% pass on new and pre-existing tests.
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-slack-integration/08-CONTEXT.md
@.planning/phases/08-slack-integration/08-01-SUMMARY.md
@.planning/phases/08-slack-integration/08-02-SUMMARY.md
@.planning/phases/08-slack-integration/08-03-SUMMARY.md
@.planning/phases/08-slack-integration/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and complete all Phase 8 test files</name>
  <files>
    tests/test_slack_manager.py,
    tests/test_huddle_recorder.py,
    tests/test_huddle_manager.py,
    tests/test_slack_socket.py
  </files>
  <action>
    Run the full test suite first to establish baseline: `.venv/bin/python -m pytest tests/ -v 2>&1 | tail -40`

    For each of the 4 test files, check which cases are already covered (from Plans 01-03 stubs) and add any missing cases below.

    **tests/test_slack_manager.py** — ensure these cases exist:
    - `test_verify_token_success`: mock `WebClient.auth_test` (no exception) → returns `(True, "")`
    - `test_verify_token_slack_error`: mock `WebClient.auth_test` raises `SlackApiError` with error code → returns `(False, error_code_str)`
    - `test_verify_token_network_error`: mock `WebClient.auth_test` raises `Exception("timeout")` → returns `(False, "timeout")`
    - `test_add_get_workspace_round_trip`: use tmp config path, add workspace with team_id T123, get_workspaces returns it
    - `test_remove_workspace`: add then remove, get_workspaces returns empty dict
    - `test_post_message_success`: mock `WebClient.chat_postMessage` → returns True
    - `test_post_message_with_blocks`: verify blocks kwarg passed through to chat_postMessage
    - `test_post_message_slack_error`: mock raises `SlackApiError` → returns False
    - `test_post_message_unknown_team`: call with team_id not in config → raises KeyError
    - `test_upload_file_success`: mock `WebClient.files_upload_v2` → returns True
    - `test_upload_file_uses_v2_not_v1`: inspect that `files_upload_v2` is called (NOT `files_upload`)

    **tests/test_huddle_recorder.py** — ensure these cases exist:
    - `test_setup_creates_null_sink_and_two_loopbacks`: mock subprocess.run capture calls list; verify 3 pactl calls in order (null-sink, loopback mic, loopback monitor)
    - `test_crash_recovery_unloads_existing_sink`: mock pulsectl to return existing sink with owner_module='42'; verify subprocess.run called with ['pactl', 'unload-module', '42'] before setup
    - `test_no_crash_recovery_when_clean`: mock pulsectl to return no sinks; verify no pre-unload subprocess call
    - `test_teardown_always_runs_on_recorder_error`: mock ConversationRecorder.start to raise RuntimeError; verify teardown subprocess calls still executed (finally block)
    - `test_stop_tears_down_modules`: verify teardown called in reverse order on stop()
    - `test_get_default_monitor_source`: mock pulsectl.Pulse.server_info().default_sink_name = 'alsa_output.foo' → returns 'alsa_output.foo.monitor'

    **tests/test_huddle_manager.py** — ensure these cases exist for `detect_activation`:
    - `test_stop_recording`: `detect_activation("conyo stop recording", "conyo")` → `("stop recording", "")`
    - `test_start_recording`: `detect_activation("conyo start recording now", "conyo")` → `("start recording", "now")`
    - `test_list_action_items`: `detect_activation("conyo list action items", "conyo")` → `("list action items", "")`
    - `test_summarize`: `detect_activation("please conyo summarize this", "conyo")` → `("summarize", "this")`
    - `test_note`: `detect_action("conyo note the budget was approved", "conyo")` → `("note", "the budget was approved")`
    - `test_topic`: `detect_activation("conyo topic sprint planning", "conyo")` → `("topic", "sprint planning")`
    - `test_help`: `detect_activation("conyo help", "conyo")` → `("help", "")`
    - `test_pause`, `test_resume`, `test_calibrate`, `test_status` — similar pattern
    - `test_no_match`: `detect_activation("hello world no activation", "conyo")` → `(None, None)`
    - `test_case_insensitive`: `detect_activation("CONYO SUMMARIZE", "conyo")` → `("summarize", "")`
    - `test_custom_word`: `detect_activation("hey summarize", "hey")` → `("summarize", "")`
    - `test_partial_match_no_false_positive`: `detect_activation("conyo unknown-command-xyz", "conyo")` → `(None, None)`

    **tests/test_slack_socket.py** — ensure these cases exist:
    - `test_start_uses_daemon_thread`: mock threading.Thread, verify daemon=True
    - `test_start_calls_connect_not_start`: verify target function is `client.connect` not `client.start`
    - `test_listener_fires_for_matching_user`: mock event with user.id == 'U123'; authed_user_id='U123' → on_huddle_event called via GLib.idle_add
    - `test_listener_ignores_wrong_user`: user.id == 'UOTHER', authed_user_id='U123' → on_huddle_event NOT called
    - `test_listener_ignores_non_huddle_event`: event type != 'user_huddle_changed' → on_huddle_event NOT called
    - `test_listener_always_acks`: even for non-huddle event, SocketModeResponse ACK is sent

    After completing all test files, run:
    ```bash
    .venv/bin/python -m pytest tests/ -v --tb=short 2>&1 | tail -50
    ```

    Fix any failures before marking done. Common issues to watch for:
    - SlackApiError constructor — use `SlackApiError(message="...", response={"error": "invalid_auth"})` in mocks
    - pulsectl mock context manager: mock as `MagicMock(__enter__=MagicMock(return_value=mock_pulse), __exit__=MagicMock(return_value=False))`
    - GLib.idle_add in tests: mock `gi.repository.GLib.idle_add` before importing modules that use it
  </action>
  <verify>
    <automated>cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass with 0 failures. New Phase 8 test files cover all specified cases. Pre-existing tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression run and test count verification</name>
  <files></files>
  <action>
    Run the complete test suite and verify:
    1. Zero failures
    2. New test count is higher than the pre-Phase-8 baseline (178 tests from Phase 7)
    3. Check for any warnings that should be fixed (DeprecationWarning for test patterns, etc.)

    ```bash
    cd /home/jon/projects/linux-speech-flow
    .venv/bin/python -m pytest tests/ -v --tb=short 2>&1
    ```

    If any test fails:
    - Fix the production code first (test failures indicate actual bugs)
    - If the test expectation is wrong (e.g., SlackApiError mock constructor), fix the test
    - Do not skip or xfail tests without explicit reason

    Record the final test count in the SUMMARY.md output.
  </action>
  <verify>
    <automated>cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. Test count is higher than 178 (Phase 7 baseline). No unexpected failures or errors.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/ -q` exits with 0 failures
- `grep -l "def test_" tests/test_slack*.py tests/test_huddle*.py | wc -l` shows 4 files
- Test count exceeds 178 (Phase 7 baseline)
</verification>

<success_criteria>
- 4 test files fully populated: test_slack_manager.py (11 cases), test_huddle_recorder.py (6 cases), test_huddle_manager.py (13+ cases), test_slack_socket.py (6 cases)
- All tests pass including pre-existing 178 Phase 7 tests
- No skipped or xfailed tests without documented reason
</success_criteria>

<output>
After completion, create `.planning/phases/08-slack-integration/08-05-SUMMARY.md`
</output>
