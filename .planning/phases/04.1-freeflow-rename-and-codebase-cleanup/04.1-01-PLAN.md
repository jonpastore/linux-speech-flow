---
phase: 04.1-freeflow-rename-and-codebase-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_speech_flow/icons/linux-speech-flow-idle.svg
  - src/linux_speech_flow/icons/linux-speech-flow-error.svg
  - src/linux_speech_flow/icons/linux-speech-flow-recording-1.svg
  - src/linux_speech_flow/icons/linux-speech-flow-recording-2.svg
  - src/linux_speech_flow/icons/linux-speech-flow-recording-3.svg
  - src/linux_speech_flow/icons/linux-speech-flow-processing-1.svg
  - src/linux_speech_flow/icons/linux-speech-flow-processing-2.svg
  - src/linux_speech_flow/icons/linux-speech-flow-processing-3.svg
  - src/linux_speech_flow/tray.py
  - src/linux_speech_flow/app.py
autonomous: true
requirements:
  - TRAY-01
  - TRAY-02
  - CONF-03

must_haves:
  truths:
    - "No 'freeflow' or 'FreeFlow' strings remain in tray.py or app.py"
    - "All 8 SVG icon files exist under linux-speech-flow-* names in src/linux_speech_flow/icons/"
    - "No freeflow-*.svg files remain in the icons/ source directory"
    - "install_icons() removes old freeflow-*.svg from ~/.local/share/icons/ and installs 8 new linux-speech-flow-*.svg files"
    - "Old ~/.config/autostart/freeflow.desktop is removed when _install_autostart() runs"
    - "New ~/.config/autostart/linux-speech-flow.desktop contains Name=Linux Speech Flow and Icon=linux-speech-flow-idle"
  artifacts:
    - path: "src/linux_speech_flow/tray.py"
      provides: "Updated icon constants — RECORDING_FRAMES, PROCESSING_FRAMES, ICON_NAMES all use linux-speech-flow-* prefix"
      contains: "linux-speech-flow-idle"
    - path: "src/linux_speech_flow/app.py"
      provides: "Updated notification titles, desktop file path, desktop Name= and Icon= fields; cleanup of old freeflow.desktop"
      contains: "Linux Speech Flow Error"
    - path: "src/linux_speech_flow/icons/linux-speech-flow-idle.svg"
      provides: "Renamed idle icon (was freeflow-idle.svg)"
  key_links:
    - from: "src/linux_speech_flow/tray.py ICON_NAMES"
      to: "src/linux_speech_flow/icons/linux-speech-flow-*.svg"
      via: "install_icons() copies files by name from ICON_NAMES list"
      pattern: "linux-speech-flow-idle.*linux-speech-flow-error"
    - from: "src/linux_speech_flow/app.py _install_autostart"
      to: "~/.config/autostart/linux-speech-flow.desktop"
      via: "writes Name= and Icon= fields using updated string literals"
      pattern: "linux-speech-flow\\.desktop"
---

<objective>
Rename all 8 SVG icon files from freeflow-* to linux-speech-flow-* using git mv, update all icon name constants in tray.py, update all FreeFlow string literals in app.py, add cleanup of old installed artifacts (orphaned icons and autostart desktop file).

Purpose: The icon rename and source code string update must be atomic — renaming SVGs without updating ICON_NAMES (or vice versa) would break the tray entirely (install_icons() would fail to find files). app.py changes are bundled here because they include the autostart desktop file cleanup, which is part of the same "installed artifacts" concern as the icon cleanup.

Output: 8 renamed SVG files (tracked via git mv), updated tray.py constants, updated app.py strings, old artifact cleanup logic embedded in install_icons() and _install_autostart().
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-CONTEXT.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-RESEARCH.md
@src/linux_speech_flow/tray.py
@src/linux_speech_flow/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename icon SVG files using git mv</name>
  <files>
    src/linux_speech_flow/icons/linux-speech-flow-idle.svg
    src/linux_speech_flow/icons/linux-speech-flow-error.svg
    src/linux_speech_flow/icons/linux-speech-flow-recording-1.svg
    src/linux_speech_flow/icons/linux-speech-flow-recording-2.svg
    src/linux_speech_flow/icons/linux-speech-flow-recording-3.svg
    src/linux_speech_flow/icons/linux-speech-flow-processing-1.svg
    src/linux_speech_flow/icons/linux-speech-flow-processing-2.svg
    src/linux_speech_flow/icons/linux-speech-flow-processing-3.svg
  </files>
  <action>
    Run git mv for each of the 8 SVG files in /home/jon/projects/linux-speech-flow/src/linux_speech_flow/icons/:

    git mv src/linux_speech_flow/icons/freeflow-idle.svg src/linux_speech_flow/icons/linux-speech-flow-idle.svg
    git mv src/linux_speech_flow/icons/freeflow-error.svg src/linux_speech_flow/icons/linux-speech-flow-error.svg
    git mv src/linux_speech_flow/icons/freeflow-recording-1.svg src/linux_speech_flow/icons/linux-speech-flow-recording-1.svg
    git mv src/linux_speech_flow/icons/freeflow-recording-2.svg src/linux_speech_flow/icons/linux-speech-flow-recording-2.svg
    git mv src/linux_speech_flow/icons/freeflow-recording-3.svg src/linux_speech_flow/icons/linux-speech-flow-recording-3.svg
    git mv src/linux_speech_flow/icons/freeflow-processing-1.svg src/linux_speech_flow/icons/linux-speech-flow-processing-1.svg
    git mv src/linux_speech_flow/icons/freeflow-processing-2.svg src/linux_speech_flow/icons/linux-speech-flow-processing-2.svg
    git mv src/linux_speech_flow/icons/freeflow-processing-3.svg src/linux_speech_flow/icons/linux-speech-flow-processing-3.svg

    Use git mv (not plain mv) so the rename is tracked in git history. This is important so pyproject.toml's "icons/*.svg" package-data glob does not pick up stale old names from git's index.
  </action>
  <verify>
    ls /home/jon/projects/linux-speech-flow/src/linux_speech_flow/icons/linux-speech-flow-*.svg | wc -l
    # Must print 8

    ls /home/jon/projects/linux-speech-flow/src/linux_speech_flow/icons/freeflow-*.svg 2>/dev/null | wc -l
    # Must print 0

    git -C /home/jon/projects/linux-speech-flow status --short | grep "^R"
    # Must show 8 rename entries (R  freeflow-*.svg -> linux-speech-flow-*.svg)
  </verify>
  <done>8 linux-speech-flow-*.svg files exist in icons/; 0 freeflow-*.svg files remain; git tracks all 8 as renames.</done>
</task>

<task type="auto">
  <name>Task 2: Update tray.py icon constants and install_icons() cleanup</name>
  <files>src/linux_speech_flow/tray.py</files>
  <action>
    Read the full current content of /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py first.

    Make these changes:

    1. Replace RECORDING_FRAMES list: change all 'freeflow-recording-*' strings to 'linux-speech-flow-recording-*'
    2. Replace PROCESSING_FRAMES list: change all 'freeflow-processing-*' strings to 'linux-speech-flow-processing-*'
    3. Replace ICON_NAMES list: change all 'freeflow-*' strings to 'linux-speech-flow-*'
       Full expected list after update:
       ICON_NAMES = [
           'linux-speech-flow-idle',
           'linux-speech-flow-recording-1', 'linux-speech-flow-recording-2', 'linux-speech-flow-recording-3',
           'linux-speech-flow-processing-1', 'linux-speech-flow-processing-2', 'linux-speech-flow-processing-3',
           'linux-speech-flow-error',
       ]
    4. Replace title='FreeFlow' (line ~51) with title='Linux Speech Flow'
    5. Replace icon_name='freeflow-idle' (line ~52) with icon_name='linux-speech-flow-idle'
    6. Replace self._tray.change_icon('freeflow-idle') (line ~77) with 'linux-speech-flow-idle'
    7. Replace self._tray.change_icon('freeflow-error') (line ~80) with 'linux-speech-flow-error'

    8. In the install_icons() function, before the loop that copies new icons, add cleanup of old freeflow-* installed files:
       OLD_ICON_STEMS = [
           'freeflow-idle', 'freeflow-error',
           'freeflow-recording-1', 'freeflow-recording-2', 'freeflow-recording-3',
           'freeflow-processing-1', 'freeflow-processing-2', 'freeflow-processing-3',
       ]
       for old_stem in OLD_ICON_STEMS:
           (dest_dir / f'{old_stem}.svg').unlink(missing_ok=True)

       Place this block immediately before the existing loop that copies icons (after dest_dir.mkdir(...) and after gtk_update_icon_cache lines if present, but before the copy loop). The old stems list can be a local variable inside install_icons(), not a module-level constant.

    Do NOT change any other logic in tray.py. The file may be long — read it fully before editing.
  </action>
  <verify>
    grep -n "freeflow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py
    # Must print nothing (zero matches)

    grep -n "Linux Speech Flow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py
    # Must show the title= line

    grep -n "linux-speech-flow-idle" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py
    # Must show at least 3 occurrences (ICON_NAMES, title init, change_icon call)

    grep -n "unlink" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py
    # Must show the old icon cleanup block
  </verify>
  <done>Zero 'freeflow' matches in tray.py; title uses 'Linux Speech Flow'; ICON_NAMES uses linux-speech-flow-* prefix; install_icons() removes old freeflow-*.svg before copying new names.</done>
</task>

<task type="auto">
  <name>Task 3: Update app.py string literals and autostart cleanup</name>
  <files>src/linux_speech_flow/app.py</files>
  <action>
    Read the full current content of /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py first.

    Make these changes (confirmed line locations from research):

    1. Line ~114: 'FreeFlow Error' → 'Linux Speech Flow Error'
    2. Line ~124: 'FreeFlow Error' → 'Linux Speech Flow Error'
       (These are notification title strings — update both occurrences)
    3. Line ~134: autostart_dir / 'freeflow.desktop' → autostart_dir / 'linux-speech-flow.desktop'
    4. Line ~138: 'Name=FreeFlow\n' → 'Name=Linux Speech Flow\n'
    5. Line ~141: 'Icon=freeflow-idle\n' → 'Icon=linux-speech-flow-idle\n'

    6. In _install_autostart(), immediately after the autostart_dir.mkdir(...) call (before writing the new desktop file), add:
       old_desktop = autostart_dir / 'freeflow.desktop'
       old_desktop.unlink(missing_ok=True)

       This removes the orphaned old autostart entry so users don't get double-launch on next login. The old file DOES exist on this machine at ~/.config/autostart/freeflow.desktop (confirmed by research).

    Do NOT change any other logic in app.py. The file may be long — read it fully before editing.
  </action>
  <verify>
    grep -n "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py
    # Must print nothing (zero matches)

    grep -n "Linux Speech Flow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py
    # Must show at least 3 lines (2 notification titles + 1 Name= field)

    grep -n "linux-speech-flow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py
    # Must show desktop file path and Icon= field lines

    grep -n "freeflow.desktop.*unlink\|unlink.*freeflow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py
    # Must show the old desktop file cleanup line
  </verify>
  <done>Zero 'freeflow' or 'FreeFlow' matches in app.py; notification titles use 'Linux Speech Flow Error'; desktop file path, Name=, and Icon= all use linux-speech-flow names; _install_autostart() removes old freeflow.desktop before writing new one.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# No freeflow strings in source code
grep -rn "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/tray.py /home/jon/projects/linux-speech-flow/src/linux_speech_flow/app.py
# Expected: no output

# 8 new icon files exist, 0 old ones
ls /home/jon/projects/linux-speech-flow/src/linux_speech_flow/icons/linux-speech-flow-*.svg | wc -l
# Expected: 8
ls /home/jon/projects/linux-speech-flow/src/linux_speech_flow/icons/freeflow-*.svg 2>/dev/null | wc -l
# Expected: 0

# Run install_icons and verify installed artifacts updated
/home/jon/projects/linux-speech-flow/.venv/bin/python -c "from linux_speech_flow.tray import install_icons; install_icons()"
ls ~/.local/share/icons/hicolor/scalable/status/linux-speech-flow-*.svg | wc -l
# Expected: 8
ls ~/.local/share/icons/hicolor/scalable/status/freeflow-*.svg 2>/dev/null | wc -l
# Expected: 0

# Verify autostart desktop updated (run install_autostart by importing app and calling method)
cat ~/.config/autostart/linux-speech-flow.desktop 2>/dev/null | grep -E "Name=|Icon="
# Expected: Name=Linux Speech Flow, Icon=linux-speech-flow-idle
ls ~/.config/autostart/freeflow.desktop 2>/dev/null
# Expected: no such file
```
</verification>

<success_criteria>
- All 8 SVG icon files exist under linux-speech-flow-* names in icons/; zero freeflow-*.svg files remain in source
- tray.py: zero 'freeflow' strings; RECORDING_FRAMES, PROCESSING_FRAMES, ICON_NAMES all use linux-speech-flow-* prefix; title='Linux Speech Flow'; install_icons() removes old freeflow-*.svg before installing new names
- app.py: zero 'freeflow' or 'FreeFlow' strings; both notification titles use 'Linux Speech Flow Error'; desktop file path/Name=/Icon= all use linux-speech-flow names; _install_autostart() removes freeflow.desktop
- After running install_icons(): 8 linux-speech-flow-*.svg present in ~/.local/share/icons/, 0 freeflow-*.svg remain
- After running _install_autostart(): linux-speech-flow.desktop present, freeflow.desktop absent from ~/.config/autostart/
</success_criteria>

<output>
After completion, create `/home/jon/projects/linux-speech-flow/.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-01-SUMMARY.md`
</output>
