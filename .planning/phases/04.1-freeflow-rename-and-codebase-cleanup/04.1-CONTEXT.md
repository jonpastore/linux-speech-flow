# Phase 4.1: FreeFlow Rename and Codebase Cleanup - Context

**Gathered:** 2026-02-21
**Status:** Ready for planning

<domain>
## Phase Boundary

Remove all "FreeFlow" / "freeflow" references from the codebase and make everything consistently `linux-speech-flow`. This phase does NOT rename the project directory, does NOT change the Python package name (`linux_speech_flow`), and does NOT introduce any new functionality. It's a naming consistency pass + git history squash.

</domain>

<decisions>
## Implementation Decisions

### Project naming convention
- Directory stays: `linux-speech-flow`
- Python package stays: `linux_speech_flow`
- CLI command stays: `linux-speech-flow`
- User-facing display name: **Linux Speech Flow** (title case, no camel-case FreeFlow)
- All "FreeFlow" and "freeflow" strings must be replaced throughout

### Icon renaming
- All 8 SVG icon files must be renamed: `freeflow-*` → `linux-speech-flow-*`
  - `freeflow-idle.svg` → `linux-speech-flow-idle.svg`
  - `freeflow-error.svg` → `linux-speech-flow-error.svg`
  - `freeflow-recording-1/2/3.svg` → `linux-speech-flow-recording-1/2/3.svg`
  - `freeflow-processing-1/2/3.svg` → `linux-speech-flow-processing-1/2/3.svg`
- All references to icon names in `tray.py` and `app.py` must be updated to match

### Source code string references
- `app.py` notifications: `'FreeFlow Error'` → `'Linux Speech Flow Error'`
- `app.py` desktop file: `'freeflow.desktop'` → `'linux-speech-flow.desktop'`
- `app.py` desktop Name: `'Name=FreeFlow\n'` → `'Name=Linux Speech Flow\n'`
- `app.py` desktop Icon: `'Icon=freeflow-idle\n'` → `'Icon=linux-speech-flow-idle\n'`
- `tray.py` title: `title='FreeFlow'` → `title='Linux Speech Flow'`
- `tray.py` all icon name constants: `freeflow-*` → `linux-speech-flow-*`

### Requirements documentation
- `REQUIREMENTS.md` title: `FreeFlow Linux` → `Linux Speech Flow`
- CONF-03: `~/.config/freeflow/config.json` → `~/.config/linux-speech-flow/config.json`
- HIST-02: `~/.local/share/freeflow/` → `~/.local/share/linux-speech-flow/`
- DIST-01: `freeflow_*.deb` → `linux-speech-flow_*.deb`
- DIST-04: `pip install freeflow-linux` → `pip install linux-speech-flow`

### Config path migration
- Add a startup migration check in `app.py` (or `config.py`): if `~/.config/freeflow/` exists, auto-migrate config to `~/.config/linux-speech-flow/` and remove the old directory
- Migration must happen before config is loaded, so config.py is the right place

### Dead code removal
- Scan all Python modules for unused imports, stubs, and commented-out code and remove

### Git history squash
- After all code changes are committed, squash all commits (currently 87 + new ones) into a single initial commit
- The squashed commit message: "Initial commit: Linux Speech Flow"
- This is the final step of the phase

### Claude's Discretion
- Exact wording of migration log message (e.g., "Migrating config from freeflow to linux-speech-flow")
- Whether to use `shutil.move` or copy+delete for config migration
- Dead code identification — Claude scans and removes what is clearly unused

</decisions>

<specifics>
## Specific Ideas

- The migration should be silent on success and log a warning on failure (don't crash if migration fails, just warn and continue with defaults)
- The git squash is the final step — all cleanup commits should be squashed together

</specifics>

<deferred>
## Deferred Ideas

- Renaming the project to `speech-flow-linux` — considered and deferred to Phase 9 (packaging) decision point
- Cross-platform support (macOS/Windows) — out of scope; app is Linux-only by design (pasimple, xdotool, X11 tray)

</deferred>

---

*Phase: 04.1-freeflow-rename-and-codebase-cleanup*
*Context gathered: 2026-02-21*
