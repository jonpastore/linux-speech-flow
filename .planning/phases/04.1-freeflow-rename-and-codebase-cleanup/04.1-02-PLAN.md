---
phase: 04.1-freeflow-rename-and-codebase-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/linux_speech_flow/config.py
  - src/linux_speech_flow/settings.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CONF-03
  - HIST-02
  - DIST-01
  - DIST-04

must_haves:
  truths:
    - "config.py contains _migrate_legacy_config() function that moves ~/.config/freeflow/ to ~/.config/linux-speech-flow/"
    - "_migrate_legacy_config() is called at the top of load_config() before any file read"
    - "Migration is silent on success and logs a warning on failure without crashing"
    - "REQUIREMENTS.md title reads 'Linux Speech Flow' not 'FreeFlow Linux'"
    - "CONF-03, HIST-02, DIST-01, DIST-04 requirement text in REQUIREMENTS.md uses linux-speech-flow paths"
    - "No unused imports remain in settings.py"
  artifacts:
    - path: "src/linux_speech_flow/config.py"
      provides: "Legacy config migration function + call in load_config()"
      contains: "_migrate_legacy_config"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement text with linux-speech-flow paths"
      contains: "Linux Speech Flow"
  key_links:
    - from: "src/linux_speech_flow/config.py load_config()"
      to: "_migrate_legacy_config()"
      via: "called at the top of load_config() before _path.exists() check"
      pattern: "_migrate_legacy_config\\(\\)"
---

<objective>
Add config path migration to config.py (handling users who had the old ~/.config/freeflow/ directory), scan and remove any unused imports from settings.py, and update REQUIREMENTS.md to replace all remaining FreeFlow-branded strings.

Purpose: config migration runs before every load_config() call — placing it there ensures no call site can skip it. The REQUIREMENTS.md update aligns documentation with the actual implementation. Dead code scan completes the codebase housekeeping.

Output: config.py with _migrate_legacy_config() wired into load_config(); settings.py with verified clean imports; REQUIREMENTS.md with corrected requirement text.
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-CONTEXT.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-RESEARCH.md
@src/linux_speech_flow/config.py
@src/linux_speech_flow/settings.py
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config migration to config.py</name>
  <files>src/linux_speech_flow/config.py</files>
  <action>
    Read the full current content of /home/jon/projects/linux-speech-flow/src/linux_speech_flow/config.py first.

    Make these changes:

    1. Add `import shutil` to the imports section (if not already present).

    2. After the existing path constants (CONFIG_DIR, CONFIG_PATH), add the legacy path constant and migration function:

       OLD_CONFIG_DIR = Path.home() / ".config" / "freeflow"

       def _migrate_legacy_config() -> None:
           """One-time migration: move ~/.config/freeflow/ to ~/.config/linux-speech-flow/."""
           if not OLD_CONFIG_DIR.exists():
               return
           try:
               if not CONFIG_PATH.exists():
                   old_config = OLD_CONFIG_DIR / "config.json"
                   if old_config.exists():
                       CONFIG_DIR.mkdir(parents=True, exist_ok=True)
                       shutil.copy2(old_config, CONFIG_PATH)
               shutil.rmtree(OLD_CONFIG_DIR, ignore_errors=True)
           except Exception as exc:
               import logging
               logging.getLogger(__name__).warning("Config migration failed: %s", exc)

    3. At the very top of the load_config() function body (before any other logic, including the _path.exists() check), insert:
       _migrate_legacy_config()

    Behavior: If ~/.config/freeflow/ does not exist, the function returns immediately (fast path for all current users). If it exists and no new config exists, copies config.json across. Then removes the old directory regardless. Silent on success, logs a warning on any exception — never raises, so load_config() always continues.

    The `_path=` keyword parameter used for test injection is untouched — the migration uses CONFIG_PATH directly (not _path), which is correct: migration checks the real path, not the test injection path.
  </action>
  <verify>
    grep -n "_migrate_legacy_config" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/config.py
    # Must show: function definition AND the call inside load_config

    grep -n "OLD_CONFIG_DIR" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/config.py
    # Must show: constant definition and usage in _migrate_legacy_config

    grep -n "import shutil" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/config.py
    # Must show shutil import

    # Verify the existing load_config tests still pass (migration is a no-op when old dir absent)
    cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/test_config.py -q 2>&1 | tail -5
    # Must show: passed, 0 failed
  </verify>
  <done>_migrate_legacy_config() defined in config.py; called as first statement of load_config(); all existing config tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Dead code scan in settings.py and REQUIREMENTS.md update</name>
  <files>
    src/linux_speech_flow/settings.py
    .planning/REQUIREMENTS.md
  </files>
  <action>
    PART A — Dead code scan in settings.py:

    Read the full content of /home/jon/projects/linux-speech-flow/src/linux_speech_flow/settings.py.

    Research identified two imports to verify:
    - `from linux_speech_flow.sounds import play_sound` — grep for all uses of `play_sound` in the file body (outside the import line itself). If zero uses found, remove the import line.
    - `from linux_speech_flow.audio import list_sinks` — grep for all uses of `list_sinks` in the file body. If zero uses found, remove the import line.

    All other imports in settings.py were confirmed live by research (math, struct, subprocess, threading, Gtk, Gdk, GLib, Gio, Pango, pulsectl, list_microphones, load_config, save_config, DEFAULT_CONFIG, validate_api_key). Do NOT remove those.

    Only remove an import if it has zero call sites in the file. If both are used, make no changes to settings.py.

    PART B — Update REQUIREMENTS.md:

    Read /home/jon/projects/linux-speech-flow/.planning/REQUIREMENTS.md.

    Make these 5 string replacements (exact lines per research):
    1. Title line: `# Requirements: FreeFlow Linux` → `# Requirements: Linux Speech Flow`
    2. CONF-03 text: `~/.config/freeflow/config.json` → `~/.config/linux-speech-flow/config.json`
    3. HIST-02 text: `~/.local/share/freeflow/` → `~/.local/share/linux-speech-flow/`
    4. DIST-01 text: `freeflow_*.deb` → `linux-speech-flow_*.deb`
    5. DIST-04 text: `pip install freeflow-linux` → `pip install linux-speech-flow`

    Per research decision: leave all other .planning/ documents (codebase/, research/, old phase plans) unchanged — they contain historical context about the macOS FreeFlow reference app and are not in scope.
  </action>
  <verify>
    # Verify REQUIREMENTS.md has no freeflow in title or requirement text
    grep -n "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/.planning/REQUIREMENTS.md
    # Acceptable remaining matches: only lines that reference old phase plan filenames (e.g. 'freeflow' in URLs or old plan refs) — the title and all requirement text must be clean

    grep -n "Linux Speech Flow" /home/jon/projects/linux-speech-flow/.planning/REQUIREMENTS.md
    # Must show: title line + at least CONF-03, HIST-02 requirement text

    # Verify settings.py still imports cleanly
    /home/jon/projects/linux-speech-flow/.venv/bin/python -c "import linux_speech_flow.settings; print('OK')"
    # Must print: OK

    # Run all tests to confirm no regressions
    cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -5
    # Must show: passed, 0 failed
  </verify>
  <done>REQUIREMENTS.md title reads 'Linux Speech Flow'; CONF-03, HIST-02, DIST-01, DIST-04 requirement text uses linux-speech-flow paths; settings.py has no unused imports and imports cleanly; all tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# Config migration present and wired
grep "_migrate_legacy_config" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/config.py | wc -l
# Expected: at least 2 (definition + call in load_config)

# REQUIREMENTS.md clean
grep -c "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/.planning/REQUIREMENTS.md
# Expected: 0 (title and requirement text are clean)

# Full test suite passes
cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -3
# Expected: all passed, 0 failed
```
</verification>

<success_criteria>
- config.py: _migrate_legacy_config() defined; called as first line of load_config(); shutil imported; all existing config tests pass
- settings.py: any confirmed unused imports removed; file still imports cleanly; all tests pass
- REQUIREMENTS.md: title is 'Linux Speech Flow'; CONF-03 path is ~/.config/linux-speech-flow/config.json; HIST-02 path is ~/.local/share/linux-speech-flow/; DIST-01 mentions linux-speech-flow_*.deb; DIST-04 mentions pip install linux-speech-flow
</success_criteria>

<output>
After completion, create `/home/jon/projects/linux-speech-flow/.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-02-SUMMARY.md`
</output>
