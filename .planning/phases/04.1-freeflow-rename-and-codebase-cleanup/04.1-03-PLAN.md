---
phase: 04.1-freeflow-rename-and-codebase-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - "04.1-01"
  - "04.1-02"
files_modified: []
autonomous: false

requirements:
  - CONF-03
  - TRAY-01

must_haves:
  truths:
    - "git log --oneline shows exactly 1 commit"
    - "That single commit message is 'Initial commit: Linux Speech Flow'"
    - "Working tree is clean after the squash (no uncommitted changes)"
    - "All source files with linux-speech-flow renames are present in the single commit"
  artifacts:
    - path: ".git"
      provides: "Single-commit git history with message 'Initial commit: Linux Speech Flow'"
      contains: "Initial commit: Linux Speech Flow"
  key_links:
    - from: "git reset --soft"
      to: "single root commit"
      via: "soft reset to root hash then amend"
      pattern: "git log --oneline | wc -l == 1"
---

<objective>
Squash the entire git history (currently 88+ commits from development) into a single initial commit. This is the final step of Phase 4.1 — all code changes from plans 01 and 02 must already be committed before this runs.

Purpose: The project was developed under "FreeFlow" branding. Squashing history removes all intermediate "FreeFlow" commit messages and presents the repository as a clean "Linux Speech Flow" project from day one. This is a one-way, destructive operation.

Output: A repository with exactly 1 commit: "Initial commit: Linux Speech Flow". All current file contents are preserved.
</objective>

<execution_context>
@/home/jon/.claude/get-shit-done/workflows/execute-plan.md
@/home/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-CONTEXT.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-RESEARCH.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-01-SUMMARY.md
@.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clean state and squash git history</name>
  <files></files>
  <action>
    Run in /home/jon/projects/linux-speech-flow.

    Step 1 — Confirm working tree is clean. If not, halt and report uncommitted changes.
    git -C /home/jon/projects/linux-speech-flow status --porcelain
    # Must be empty output. If not empty, do NOT proceed.

    Step 2 — Confirm commit count before squash (should be 88 or more):
    git -C /home/jon/projects/linux-speech-flow log --oneline | wc -l

    Step 3 — Get the root commit hash:
    ROOT=$(git -C /home/jon/projects/linux-speech-flow rev-list --max-parents=0 HEAD)
    echo "Root commit: $ROOT"

    Step 4 — Soft reset to root (all changes since root become staged, root commit content remains):
    git -C /home/jon/projects/linux-speech-flow reset --soft "$ROOT"

    Step 5 — Amend the root commit with the correct message:
    git -C /home/jon/projects/linux-speech-flow commit --amend -m "Initial commit: Linux Speech Flow"

    CRITICAL NOTES:
    - This is IRREVERSIBLE. The old commits will be garbage collected eventually. Do not run if working tree is dirty.
    - Do NOT push to any remote (there is no remote configured on this repo — local only).
    - Do NOT use --no-verify or any flag that skips hooks.
    - The `git commit --amend` rewrites only the root commit's message + adds all the staged changes from the soft reset. All file contents are preserved exactly.
  </action>
  <verify>
    git -C /home/jon/projects/linux-speech-flow log --oneline | wc -l
    # Must print: 1

    git -C /home/jon/projects/linux-speech-flow log --oneline
    # Must print: <hash> Initial commit: Linux Speech Flow

    git -C /home/jon/projects/linux-speech-flow status --porcelain
    # Must be empty (clean working tree)

    # Confirm the old freeflow-*.svg files are not tracked anymore
    git -C /home/jon/projects/linux-speech-flow ls-files src/linux_speech_flow/icons/ | grep freeflow
    # Must be empty
  </verify>
  <done>git log --oneline shows exactly 1 commit with message 'Initial commit: Linux Speech Flow'; working tree is clean; no freeflow-*.svg files tracked by git.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 4.1 cleanup</name>
  <files></files>
  <action>
    What was automated across plans 01–03:
    - 8 SVG icons renamed freeflow-* to linux-speech-flow-* via git mv
    - tray.py: all ICON_NAMES, RECORDING_FRAMES, PROCESSING_FRAMES constants updated; title changed; install_icons() cleans up old freeflow-*.svg from installed location
    - app.py: notification titles, desktop file path, Name=, Icon= all updated; _install_autostart() removes old freeflow.desktop
    - config.py: _migrate_legacy_config() added and called from load_config()
    - REQUIREMENTS.md: title and requirement text updated to linux-speech-flow
    - Git history squashed to single commit: "Initial commit: Linux Speech Flow"

    Run these checks:

    1. git log --oneline
       Expected: exactly 1 line — "<hash> Initial commit: Linux Speech Flow"

    2. grep -rn "freeflow\|FreeFlow" src/linux_speech_flow/
       Expected: no output

    3. ls src/linux_speech_flow/icons/
       Expected: all files named linux-speech-flow-*.svg, none named freeflow-*

    4. Launch the app and confirm tray icon appears:
       .venv/bin/linux-speech-flow &
       Expected: tray icon appears in system tray, no error notification, icon is visible

    5. cat ~/.config/autostart/linux-speech-flow.desktop | grep -E "Name=|Icon="
       Expected: Name=Linux Speech Flow, Icon=linux-speech-flow-idle

    6. ls ~/.config/autostart/freeflow.desktop 2>/dev/null && echo "PROBLEM" || echo "OK: gone"
       Expected: OK: gone

    7. ls ~/.local/share/icons/hicolor/scalable/status/ | grep freeflow
       Expected: no output (old icons removed by install_icons())
  </action>
  <verify>
    grep -rn "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/src/linux_speech_flow/
    # Expected: no output

    git -C /home/jon/projects/linux-speech-flow log --oneline | wc -l
    # Expected: 1
  </verify>
  <done>User confirms app launches with correct tray icon; no freeflow strings in source; git history is single commit; old installed artifacts removed.</done>
</task>

</tasks>

<verification>
Final state verification:

```bash
# Single commit
git -C /home/jon/projects/linux-speech-flow log --oneline | wc -l
# Expected: 1

# Correct message
git -C /home/jon/projects/linux-speech-flow log --oneline
# Expected: <hash> Initial commit: Linux Speech Flow

# No freeflow strings in codebase source files
grep -rn "freeflow\|FreeFlow" /home/jon/projects/linux-speech-flow/src/
# Expected: no output

# No old icon files tracked
git -C /home/jon/projects/linux-speech-flow ls-files src/linux_speech_flow/icons/ | grep freeflow
# Expected: no output

# All tests pass
cd /home/jon/projects/linux-speech-flow && .venv/bin/python -m pytest tests/ -q 2>&1 | tail -3
# Expected: all passed
```
</verification>

<success_criteria>
- Git history contains exactly 1 commit with message "Initial commit: Linux Speech Flow"
- Working tree clean after squash
- No freeflow/FreeFlow strings in any file under src/linux_speech_flow/
- No freeflow-*.svg files tracked by git in icons/
- App launches correctly with tray icon visible (human verified)
- Old autostart entry removed; new one has correct Name= and Icon=
- All tests pass
</success_criteria>

<output>
After completion, create `/home/jon/projects/linux-speech-flow/.planning/phases/04.1-freeflow-rename-and-codebase-cleanup/04.1-03-SUMMARY.md`
</output>
