---
phase: 04.1-freeflow-rename-and-codebase-cleanup
plan: "02"
subsystem: config-migration-and-docs
tags: [config, migration, cleanup, requirements]
dependency_graph:
  requires: []
  provides: [legacy-config-migration, requirements-branding-update]
  affects: [src/linux_speech_flow/config.py, .planning/REQUIREMENTS.md]
tech_stack:
  added: [shutil]
  patterns: [one-time migration at load time, local imports in methods]
key_files:
  created: []
  modified:
    - src/linux_speech_flow/config.py
    - .planning/REQUIREMENTS.md
decisions:
  - "shutil.copy2 + rmtree for migration: atomic enough for config files, ignores errors to never crash load_config()"
  - "import logging inside except block: avoids circular import risk and keeps it lazy (migration failure is rare)"
  - "settings.py: no top-level dead imports found — play_sound and list_sinks are local imports inside methods, correctly scoped"
  - "REQUIREMENTS.md: only title + 4 requirement text lines changed; historical planning docs left unchanged per research decision"
metrics:
  duration: 1 min
  completed_date: "2026-02-21"
  tasks_completed: 2
  files_changed: 2
---

# Phase 4.1 Plan 02: Config Migration and Requirements Cleanup Summary

Config path migration from ~/.config/freeflow/ to ~/.config/linux-speech-flow/ wired into load_config(), plus REQUIREMENTS.md branding updated from FreeFlow Linux to Linux Speech Flow with corrected paths.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add config migration to config.py | e3005aa | src/linux_speech_flow/config.py |
| 2 | Dead code scan in settings.py + REQUIREMENTS.md update | 1ba5ddf | .planning/REQUIREMENTS.md |

## What Was Built

### Task 1: Config migration in config.py

Added to `src/linux_speech_flow/config.py`:
- `import shutil` at module level
- `OLD_CONFIG_DIR = Path.home() / ".config" / "freeflow"` constant
- `_migrate_legacy_config()` function: checks if old dir exists, copies config.json if new path doesn't exist, removes old dir with `ignore_errors=True`
- Call to `_migrate_legacy_config()` as the first statement in `load_config()`

Fast path: if `~/.config/freeflow/` does not exist, function returns immediately — zero overhead for current users.

### Task 2: Settings.py dead code scan

Confirmed no top-level dead imports in `settings.py`:
- `play_sound` is imported locally inside `_on_play_sound()` (line 544) — correctly scoped, not a dead import
- `list_sinks` is imported locally inside `_enumerate_sinks()` (line 404) — correctly scoped, not a dead import

No changes made to `settings.py`.

### Task 2: REQUIREMENTS.md branding update

5 string replacements applied:
1. Title: `# Requirements: FreeFlow Linux` → `# Requirements: Linux Speech Flow`
2. CONF-03: `~/.config/freeflow/config.json` → `~/.config/linux-speech-flow/config.json`
3. HIST-02: `~/.local/share/freeflow/` → `~/.local/share/linux-speech-flow/`
4. DIST-01: `freeflow_*.deb` → `linux-speech-flow_*.deb`
5. DIST-04: `pip install freeflow-linux` → `pip install linux-speech-flow`

## Verification

```
_migrate_legacy_config occurrences in config.py: 2 (definition + call)
freeflow/FreeFlow occurrences in REQUIREMENTS.md: 0
Full test suite: 22 passed, 0 failed
```

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED

- [x] `src/linux_speech_flow/config.py` exists and contains `_migrate_legacy_config`
- [x] `.planning/REQUIREMENTS.md` title reads "Linux Speech Flow"
- [x] Commits e3005aa and 1ba5ddf exist
- [x] 22 tests pass
